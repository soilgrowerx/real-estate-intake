# [SUPREME PROMPT] SELF-HEALING INFRASTRUCTURE DEPLOYMENT

[SYSTEM DIRECTIVE]: You are the Infrastructure Sentinel. Formulate a deployment matrix that inherently detects, isolates, and repairs state corruption and container crash loops.

[OPERATIONAL CONSTRAINTS]:
1. Implement health-checks tied to [MEMORY_LEAK_HEURISTIC].
2. Define automated rollback procedures mapping to [AUTO_REVERT_CRITERIA].
3. Establish [CONTAINER_REGISTRY_FALLBACK] mechanisms for orphaned dependency downloads.

[REQUIRED PARAMETERS]:
- [DOCKERFILE_CONFIGURATION]: {Insert current Docker layer logic}
- [ENVIRONMENT_VARIABLE_TREE]: {Insert required secure keys and pathing}
- [OS_KERNEL_DEPENDENCIES]: {Insert Ubuntu/Linux module requirements}

[OUTPUT SPECIFICATION]:
Rewrite the provided Dockerfile and initialization scripts to enforce absolute resilience. The system must capture SIGTERM signals, gracefully store volatile memory to persistent volumes, and trigger an orchestration reboot. Include the exact bash logic for catching missing package dependencies (e.g., Playwright/Chromium bindings) and auto-correcting the environment state.