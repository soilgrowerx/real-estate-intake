## 2026-03-25 - System Restoration & Decommissioning

- **Soil Grower Bot Decommissioned:** The old Soil Grower bot account (marked as 'default') has been severed from the OpenClaw configuration to stop message bleed-over. The pecanpi account is now the primary gateway for all Telegram functions.
- **Manual Chat Archival:** Automated history fetch for the old bot failed due to an expired API token. George will manually export the chat history from Telegram Desktop to `/home/george/Documents/Compost_Pile/` for future ingestion.
- **API Rate Limit Mitigation:** Systemic Google API rate limits (429 errors) caused silence in group topics. I have shifted the "Soil Grower Ecosystem" group topic to use `ollama/glm4:latest` (local compute) to bypass external nutrient shortages.
- **Config Correction:** Executed `openclaw doctor --fix` to consolidate Telegram account structures and resolve minor configuration drifts.
- **Mozi Insight:** Captured the Hormozi "Rule of 100" (Volume Negates Luck) to the Compost Pile for the daily ingestion cycle.

## OpenClaw Ecosystem Restoration & Model Tuning (March 25, 2026)
- **Local Model Fix:** Restored Ollama functionality by injecting a dummy `OLLAMA_API_KEY="ollama-local"` into the environment. This was required by the v3.24 update for provider registration.
- **Model Hierarchy:** 
    - **Global Default:** Set to `ollama/glm4:latest` (local-heavy) to conserve cloud nutrients.
    - **Architectural Core:** `main-agent`, `Boardy`, `Thea`, and `Quercus` are explicitly locked to `google-gemini-cli/gemini-3.1-pro-preview` for deep reasoning.
    - **Operational Agents:** `Bodhi` moved to `glm4`; `Sylvan` moved to `llama3.2:3b`.
- **Group Chat Restoration:** Resolved silent dropping of group messages by setting `channels.telegram.groupPolicy` to `open` (previously `allowlist` with an empty list).
- **Semantic Memory:** Migrated `memorySearch` provider to `ollama` using `nomic-embed-text:latest`. This enables local vector embeddings and resolves cloud-dependency warnings in `openclaw doctor`.
- **Quality Guard:** Enabled `compaction.qualityGuard` to ensure high-fidelity context summaries during memory flushes.
